# 001 Section Intro

> can skip.

# 002 Hello World!

> use alert to show hello word.
> define variable let js = amazing
> now check if js value is amazing so alert javascript is amazing
> now set js boring
> now check if js value is amazing so alert javascript is amazing
> we can do some math also in javascript like 48+11-22

# 003 A Brief Introduction to JavaScript

> can skip.

# 004 Linking a JavaScript File

> can skip. only showing how to write code in js file and link file

# 005 Values and Variables

> store values into variables
> let firstName = 'usama'
> let lastName = 'ameer'
> than log the name
> log firstName 3 times so it will show 3 times no need to change name everywhere just change one place

# 007 Data Types

> there are 3 most important data types number,string,boolean
> define variable and set value true
> let javascriptIsFun = true;
> cl (typeof javascriptIsFun)
> define a variable let year but not assign value so than check type of it will be undefine
> typeof null is object

# 008 let, const and var

> can skip. i know about let const and var

# 009 Basic Operators

> x=10
> x+=5 it is like x= x+5 =15;
> x*=5 it means x = x*5 =50

# 010 Operator Precedence

> can skip i know it.

# 013 CHALLENGE #1 Video Solution

> do it from challenge pdf

# 014 Strings and Template Literals

> we need to print using template literals
> like i am usama and i am working in albiada as a accountant from x years
> const dateOfBirthUsama = 1992;
> const username = "usamaameer";
> let job = `Accountant`;
> cl`My name is ${username} i am ${2026 - dateOfBirthUsama} years i am working as ${job}`

# 015 Taking Decisions if else Statements

> create variable age in store age is 18
> create variable isOldEnough = age>=18
> if isOldEnough true sara can drive else sara has to wait in years
> next exercise is
> create variable birthYear = 1991
> now check if birthYear is <=2000 is 20th century else century is 21
> log the century of birthYear

# 018 Type Conversion and Coercion

> type conversion is when we manual convert one type into another type like
> let age ='22'; this is type of string
> we need to put Number() then we can manual change it
> or some time we do age \*1 so it is coercion when javascript auto change its type
> if we try to do Number('usama') it will come NaN it mean invalid number
> same like we can convert number into string String(123)

# 019 Truthy and Falsy Values

> there are only 5 falsy values in javascript
> 0 , " " empty string , undefine , null ,NaN
> when we want to convert them in boolean it will be falsy values

# 020 Equality Operators == vs. ===

> can skip

# 021 Boolean Logic

> can skip

# 022 Logical Operators

> watch video

# 025 The switch Statement

> watch video

# 027 The Conditional (Ternary) Operator

>const isMature = age > 30 ? "Your are mature" : "Not mature";

# 
