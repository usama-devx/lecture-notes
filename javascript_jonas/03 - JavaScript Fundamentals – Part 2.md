# 001 Section Intro

> can skip

# 002 Activating Strict Mode

> use strict mode always

# 003 Functions

> piece of code that we use over and over again in code
> can skip i know it.

# 004 Function Declarations vs. Expressions

> function has two parts one is declaration other is expression
> declaration is like we write simple function
> expression is like we use variable to store function like arrow function

# 005 Arrow Functions

> create arrow function to calculate age.
> create arrow function to calculate yearUntilRetirement

# 006 Functions Calling Other Functions

> that call one function to another function

# 007 Reviewing Functions

> if number is > 0 return value else return -1 cl you are already retied

# 010 Introduction to Arrays

> create array with your friends name
> create array hold your friends birthYears
> array.lenght will give you elements in the array
> try to get last element of array
> in array we can put variable operation and another array

# 012 CHALLENGE #2

> done ✅

# 014 Introduction to Objects

> in object we can store key value pair

# 015 Dot vs. Bracket Notation

> we can get values from object by its property name by two ways
> one is dot notation in that we put usamaObj.name like this
> create a question and and button to get details about obj
> as we can get data with usama.name same as we can store data usama.location = 'qatar';

# 016 Object Methods (watch video very imp for learing this and obj)

> define one more property in object hasDrivingLicense
> also define one method in object calculateAge
> very import concept we need to note with method we can set property in side method
> this.age = 2026 - this.birthYear;

# 017 CHALLENGE #3

> done ✅

# 018 done

# 019 Iteration The for Loop

> simple for loop can skip

# 020 Looping Arrays, Breaking and Continuing
> create one array with name age username fname lname job friends hasDrivingLicense
> then log each element using for loop
> create one empty array type of in that store there type
> log the types
> push types using push and simple method also arrary[0]=
> create array with birthyears 
> create empty array hold the ages
> also use break and continue concepts in if else